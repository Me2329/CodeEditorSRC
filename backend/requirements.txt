# CodeCraft Studio - gateway runtime dependencies.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
# Optional: the gateway falls back to an in-process rate limiter when Redis is
# unreachable, so a single-node deployment needs no external services.
redis>=5.0,<7.0
